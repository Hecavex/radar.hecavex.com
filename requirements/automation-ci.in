# CI adds linting and type-checking tools declared in pyproject.toml to the runtime lock.
-r automation-runtime.in

hypothesis==6.167.1
mypy==2.3.1
pytest==9.1.1
ruff==0.16.5
# The 3.3.1 wheel omits the bundled schemas required by validate_file().
stix2-validator==3.2.0
